# Example only: copy this file to secret.yaml and replace every placeholder
# before applying it. Do not commit secret.yaml or any real credentials.
apiVersion: v1
kind: Secret
metadata:
  name: projectpulse-secret
  labels:
    app.kubernetes.io/part-of: projectpulse
type: Opaque
stringData:
  APP_KEY: "REPLACE_WITH_LARAVEL_APP_KEY"
  DB_USERNAME: "REPLACE_WITH_DATABASE_USERNAME"
  DB_PASSWORD: "REPLACE_WITH_DATABASE_PASSWORD"
  LLM_API_KEY: "REPLACE_WITH_LLM_API_KEY"
